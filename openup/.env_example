SECRET_KEY=***REMOVED_DJANGO_SECRET***

DATABASE_NAME=openup
DATABASE_USER=user_name
DATABASE_PASS=pass_here
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306

FCM_SERVER_KEY11='***REMOVED_FCM_KEY***'
STRIPE_KEY="***REMOVED_STRIPE_PK***"
STRIPE_SECRET="***REMOVED_STRIPE_SK***"
STRIPE_API="***REMOVED_STRIPE_SK***"
BASE_URL='http://192.168.1.5:8000/api'
MAIL_URL='192.168.1.5:8000'

# Venmo is processed through PayPal's Orders API (payment_source.venmo),
# reusing the existing CLIENT_ID / CLIENT_SECRET PayPal app credentials.
PAYPAL_BASE_URL='https://api-m.sandbox.paypal.com'
PAYPAL_BRAND_NAME='YourBrandName'
VENMO_RETURN_URL='https://example.com/returnUrl'
VENMO_CANCEL_URL='https://example.com/cancelUrl'


# ALTER TABLE `users` ADD COLUMN `user_fcm_access_key` longtext NULL;

# Run `python manage.py migrate` to apply 0062_venmoinfo.py, or create the
# table manually with the equivalent SQL:
# CREATE TABLE `venmo_cust_info` (
#   `venmo_info_id` INT AUTO_INCREMENT PRIMARY KEY,
#   `venmo_vault_id` VARCHAR(50) NOT NULL,
#   `venmo_cust_id` VARCHAR(100) NOT NULL,
#   `venmo_email` VARCHAR(150) NULL,
#   `venmo_response` LONGTEXT NOT NULL,
#   `is_delete` BOOL NOT NULL,
#   `created_at` DATETIME(6) NOT NULL,
#   `update_at` DATETIME(6) NULL,
#   `venmo_user_id` INT NOT NULL,
#   CONSTRAINT `venmo_cust_info_venmo_user_id_fk` FOREIGN KEY (`venmo_user_id`) REFERENCES `users` (`user_id`)
# );